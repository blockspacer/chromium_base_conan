// mojo/public/interfaces/bindings/tests/echo.mojom.js is auto generated by mojom_bindings_generator.py, do not edit

// Copyright 2014 The Chromium Authors. All rights reserved.
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

'use strict';

(function() {
  var mojomId = 'mojo/public/interfaces/bindings/tests/echo.mojom';
  if (mojo.internal.isMojomLoaded(mojomId)) {
    console.warn('The following mojom is loaded multiple times: ' + mojomId);
    return;
  }
  mojo.internal.markMojomLoaded(mojomId);
  var bindings = mojo;
  var associatedBindings = mojo;
  var codec = mojo.internal;
  var validator = mojo.internal;

  var exports = mojo.internal.exposeNamespace('test.echo.mojom');
  var echo_import$ =
      mojo.internal.exposeNamespace('test.echoImport.mojom');
  if (mojo.config.autoLoadMojomDeps) {
    mojo.internal.loadMojomIfNecessary(
        'mojo/public/interfaces/bindings/tests/echo_import/echo_import.mojom', 'echo_import/echo_import.mojom.js');
  }



  function Echo_EchoPoint_Params(values) {
    this.initDefaults_();
    this.initFields_(values);
  }


  Echo_EchoPoint_Params.prototype.initDefaults_ = function() {
    this.point = null;
  };
  Echo_EchoPoint_Params.prototype.initFields_ = function(fields) {
    for(var field in fields) {
        if (this.hasOwnProperty(field))
          this[field] = fields[field];
    }
  };

  Echo_EchoPoint_Params.validate = function(messageValidator, offset) {
    var err;
    err = messageValidator.validateStructHeader(offset, codec.kStructHeaderSize);
    if (err !== validator.validationError.NONE)
        return err;

    var kVersionSizes = [
      {version: 0, numBytes: 16}
    ];
    err = messageValidator.validateStructVersion(offset, kVersionSizes);
    if (err !== validator.validationError.NONE)
        return err;


    // validate Echo_EchoPoint_Params.point
    err = messageValidator.validateStructPointer(offset + codec.kStructHeaderSize + 0, echo_import$.Point, false);
    if (err !== validator.validationError.NONE)
        return err;

    return validator.validationError.NONE;
  };

  Echo_EchoPoint_Params.encodedSize = codec.kStructHeaderSize + 8;

  Echo_EchoPoint_Params.decode = function(decoder) {
    var packed;
    var val = new Echo_EchoPoint_Params();
    var numberOfBytes = decoder.readUint32();
    var version = decoder.readUint32();
    val.point =
        decoder.decodeStructPointer(echo_import$.Point);
    return val;
  };

  Echo_EchoPoint_Params.encode = function(encoder, val) {
    var packed;
    encoder.writeUint32(Echo_EchoPoint_Params.encodedSize);
    encoder.writeUint32(0);
    encoder.encodeStructPointer(echo_import$.Point, val.point);
  };
  function Echo_EchoPoint_ResponseParams(values) {
    this.initDefaults_();
    this.initFields_(values);
  }


  Echo_EchoPoint_ResponseParams.prototype.initDefaults_ = function() {
    this.result = null;
  };
  Echo_EchoPoint_ResponseParams.prototype.initFields_ = function(fields) {
    for(var field in fields) {
        if (this.hasOwnProperty(field))
          this[field] = fields[field];
    }
  };

  Echo_EchoPoint_ResponseParams.validate = function(messageValidator, offset) {
    var err;
    err = messageValidator.validateStructHeader(offset, codec.kStructHeaderSize);
    if (err !== validator.validationError.NONE)
        return err;

    var kVersionSizes = [
      {version: 0, numBytes: 16}
    ];
    err = messageValidator.validateStructVersion(offset, kVersionSizes);
    if (err !== validator.validationError.NONE)
        return err;


    // validate Echo_EchoPoint_ResponseParams.result
    err = messageValidator.validateStructPointer(offset + codec.kStructHeaderSize + 0, echo_import$.Point, false);
    if (err !== validator.validationError.NONE)
        return err;

    return validator.validationError.NONE;
  };

  Echo_EchoPoint_ResponseParams.encodedSize = codec.kStructHeaderSize + 8;

  Echo_EchoPoint_ResponseParams.decode = function(decoder) {
    var packed;
    var val = new Echo_EchoPoint_ResponseParams();
    var numberOfBytes = decoder.readUint32();
    var version = decoder.readUint32();
    val.result =
        decoder.decodeStructPointer(echo_import$.Point);
    return val;
  };

  Echo_EchoPoint_ResponseParams.encode = function(encoder, val) {
    var packed;
    encoder.writeUint32(Echo_EchoPoint_ResponseParams.encodedSize);
    encoder.writeUint32(0);
    encoder.encodeStructPointer(echo_import$.Point, val.result);
  };
  var kEcho_EchoPoint_Name = 603489257;

  function EchoPtr(handleOrPtrInfo) {
    this.ptr = new bindings.InterfacePtrController(Echo,
                                                   handleOrPtrInfo);
  }

  function EchoAssociatedPtr(associatedInterfacePtrInfo) {
    this.ptr = new associatedBindings.AssociatedInterfacePtrController(
        Echo, associatedInterfacePtrInfo);
  }

  EchoAssociatedPtr.prototype =
      Object.create(EchoPtr.prototype);
  EchoAssociatedPtr.prototype.constructor =
      EchoAssociatedPtr;

  function EchoProxy(receiver) {
    this.receiver_ = receiver;
  }
  EchoPtr.prototype.echoPoint = function() {
    return EchoProxy.prototype.echoPoint
        .apply(this.ptr.getProxy(), arguments);
  };

  EchoProxy.prototype.echoPoint = function(point) {
    var params_ = new Echo_EchoPoint_Params();
    params_.point = point;
    return new Promise(function(resolve, reject) {
      var builder = new codec.MessageV1Builder(
          kEcho_EchoPoint_Name,
          codec.align(Echo_EchoPoint_Params.encodedSize),
          codec.kMessageExpectsResponse, 0);
      builder.encodeStruct(Echo_EchoPoint_Params, params_);
      var message = builder.finish();
      this.receiver_.acceptAndExpectResponse(message).then(function(message) {
        var reader = new codec.MessageReader(message);
        var responseParams =
            reader.decodeStruct(Echo_EchoPoint_ResponseParams);
        resolve(responseParams);
      }).catch(function(result) {
        reject(Error("Connection error: " + result));
      });
    }.bind(this));
  };

  function EchoStub(delegate) {
    this.delegate_ = delegate;
  }
  EchoStub.prototype.echoPoint = function(point) {
    return this.delegate_ && this.delegate_.echoPoint && this.delegate_.echoPoint(point);
  }

  EchoStub.prototype.accept = function(message) {
    var reader = new codec.MessageReader(message);
    switch (reader.messageName) {
    default:
      return false;
    }
  };

  EchoStub.prototype.acceptWithResponder =
      function(message, responder) {
    var reader = new codec.MessageReader(message);
    switch (reader.messageName) {
    case kEcho_EchoPoint_Name:
      var params = reader.decodeStruct(Echo_EchoPoint_Params);
      this.echoPoint(params.point).then(function(response) {
        var responseParams =
            new Echo_EchoPoint_ResponseParams();
        responseParams.result = response.result;
        var builder = new codec.MessageV1Builder(
            kEcho_EchoPoint_Name,
            codec.align(Echo_EchoPoint_ResponseParams.encodedSize),
            codec.kMessageIsResponse, reader.requestID);
        builder.encodeStruct(Echo_EchoPoint_ResponseParams,
                             responseParams);
        var message = builder.finish();
        responder.accept(message);
      });
      return true;
    default:
      return false;
    }
  };

  function validateEchoRequest(messageValidator) {
    var message = messageValidator.message;
    var paramsClass = null;
    switch (message.getName()) {
      case kEcho_EchoPoint_Name:
        if (message.expectsResponse())
          paramsClass = Echo_EchoPoint_Params;
      break;
    }
    if (paramsClass === null)
      return validator.validationError.NONE;
    return paramsClass.validate(messageValidator, messageValidator.message.getHeaderNumBytes());
  }

  function validateEchoResponse(messageValidator) {
   var message = messageValidator.message;
   var paramsClass = null;
   switch (message.getName()) {
      case kEcho_EchoPoint_Name:
        if (message.isResponse())
          paramsClass = Echo_EchoPoint_ResponseParams;
        break;
    }
    if (paramsClass === null)
      return validator.validationError.NONE;
    return paramsClass.validate(messageValidator, messageValidator.message.getHeaderNumBytes());
  }

  var Echo = {
    name: 'test.echo.mojom.Echo',
    kVersion: 0,
    ptrClass: EchoPtr,
    proxyClass: EchoProxy,
    stubClass: EchoStub,
    validateRequest: validateEchoRequest,
    validateResponse: validateEchoResponse,
  };
  EchoStub.prototype.validator = validateEchoRequest;
  EchoProxy.prototype.validator = validateEchoResponse;
  exports.Echo = Echo;
  exports.EchoPtr = EchoPtr;
  exports.EchoAssociatedPtr = EchoAssociatedPtr;
})();